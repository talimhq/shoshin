<% content_for :title, "Modifier une question" %>
<% content_for :parent_url, url_for([:teacher, @question.exercise]) %>
<div class="row">
  <div class="column xs12 l8 offset-l2">
    <div class="card">
      <div class="title-area">
        <h3 class="main-title">Modifier une question</h3>
      </div>
      <%= render 'form' %>
    </div>
  </div>
</div>
