<% content_for :title, "Modifier un cours" %>
<% content_for :parent_url, teacher_lesson_path(@lesson) %>
<div class="row">
  <div class="column xs12 l8 offset-l2">
    <div class="card">
      <div class="title-area">
        <h3 class="main-title">Modifier un cours</h3>
      </div>
      <%= render 'form' %>
    </div>
  </div>
</div>
