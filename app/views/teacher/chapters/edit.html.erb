<% content_for :title, "#{@chapter.name} - Modifier" %>
<% content_for :parent_url, url_for([:teacher, @chapter.group]) %>
<div class="row">
  <div class="column">
    <div class="card">
      <div class="title-area">
        <h3 class="main-title">Modifier un chapitre</h3>
      </div>
      <%= render 'form' %>
    </div>
  </div>
</div>
