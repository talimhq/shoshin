<% content_for :title, "Nouvelle seance" %>
<% content_for :parent_url, teacher_lesson_path(@step.lesson) %>
<div class="row">
  <div class="column xs12 l8 offset-l2">
    <div class="card">
      <div class="title-area">
        <h3 class="main-title">Nouvelle séance</h3>
      </div>
      <%= render 'form' %>
    </div>
  </div>
</div>
