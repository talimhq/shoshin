<% content_for :title, 'Ajouter une classe' %>
<% content_for :parent_url, url_for([:teacher, @classroom.school]) %>
<div class="row">
  <div class="column">
    <div class="card">
      <%= render 'form' %>
    </div>
  </div>
</div>
