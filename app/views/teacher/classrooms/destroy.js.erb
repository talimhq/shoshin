$('body').append('<%= j render partial: 'layouts/flash_messages' %>');
$('.toast-container').hide().slideToggle('slow').delay(5000).slideToggle('slow');
<% unless @classroom.persisted? %>
  $('#classroom_<%= j @classroom.id.to_s %>').remove();
<% end %>
