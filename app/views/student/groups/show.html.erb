<%= content_for :title, @group.display_name %>
<div class="page-title">
  <div class="title">
    <%= @group.display_name %>
  </div>
</div>
<div class="row">
  <div class="column xs12 s8 m12 l8">
    <div class="card">
      <div class="table-area">
        <div class="header">
          Chapitres
        </div>
        <table>
          <thead>
            <tr>
              <th class="text">Titre</th>
              <th class="actions"></th>
            </tr>
          </thead>
          <tbody>
            <% @group.chapters.each do |chapter| %>
              <tr>
                <td class="text"><%= chapter.name %></td>
                <td class="actions">
                  <%= link_to material_icon.visibility, [:student, chapter],class: 'btn' %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
