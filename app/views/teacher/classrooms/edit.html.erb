<% content_for :title, 'Modifier une classe' %>
<% content_for :parent_url, url_for([:teacher, @classroom]) %>
<div class="row">
  <div class="column">
    <div class="card">
      <%= render 'form' %>
    </div>
  </div>
</div>
