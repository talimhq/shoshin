<% content_for :title, "Modifier un exercice" %>
<% content_for :parent_url, url_for([:teacher, @exercise]) %>
<div class="row">
  <div class="column xs12 l8 offset-l2">
    <div class="card">
      <div class="title-area">
        <h3 class="main-title">Modifier un exercice</h3>
      </div>
      <%= render 'form' %>
    </div>
  </div>
</div>
